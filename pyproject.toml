[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "self-model-notes"
version = "0.1.0"
description = "Evidence-traceable dynamic Self Model knowledge base"
requires-python = ">=3.11"
dependencies = ["PyYAML>=6.0,<7"]

[tool.setuptools]
py-modules = []

