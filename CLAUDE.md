# Claude Execution Entry

最初に `AGENTS.md` を全文読み、次にIssue #1、`docs/schema.md`、`execution/tasks.yaml`の対象タスクを読むこと。

- 1回に1タスクだけ実行する。
- `allowed_paths`外を変更しない。
- 推測でUnknownを埋めない。
- 要件や語彙の変更が必要なら実装せずIssueへ提案する。
- 完了条件と検証コマンドは `AGENTS.md` に従う。

